name: issue94-mcp-acceptance

on:
  push:
    paths:
      - '.github/scripts/issue94_acceptance_patch.py'
      - '.github/workflows/issue94-acceptance.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply MCP acceptance migration
        run: python .github/scripts/issue94_acceptance_patch.py
      - name: Format and verify
        run: |
          cargo fmt --all -- --check
          cargo clippy --locked --all-targets --all-features -- -D warnings
          cargo test --locked --all-targets
      - name: Commit implementation and remove temporary helpers
        run: |
          rm .github/scripts/issue94_acceptance_patch.py
          rm .github/workflows/issue94-acceptance.yml
          git config user.name Cheng
          git config user.email chiacheng.tsao@gmail.com
          git add -A
          git commit -m "#94 Complete MCP authorization acceptance coverage"
          git push origin HEAD:main
