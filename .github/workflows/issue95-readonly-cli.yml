name: issue95-readonly-cli

on:
  push:
    paths:
      - '.github/scripts/issue95_readonly_cli_patch.py'
      - '.github/workflows/issue95-readonly-cli.yml'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: Apply strict read-only CLI repair
        run: python .github/scripts/issue95_readonly_cli_patch.py
      - name: Format and verify
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
          cargo clippy --locked --all-targets --all-features -- -D warnings
          cargo test --locked --all-targets
      - name: Commit implementation and remove temporary helpers
        run: |
          rm .github/scripts/issue95_readonly_cli_patch.py
          rm .github/workflows/issue95-readonly-cli.yml
          git config user.name Cheng
          git config user.email chiacheng.tsao@gmail.com
          git add -A
          git commit -m "#95 Enforce read-only execution audit connections"
          git push origin HEAD:main
